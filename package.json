{
  "name": "king-bolete-forecast",
  "version": "1.0.0",
  "private": true,
  "description": "King bolete foraging forecast for Washington State. The app itself is a single static index.html with no build step; this file exists only to wire up the test suites.",
  "scripts": {
    "test": "npm run test:model && npm run test:data",
    "test:model": "node --test tests/model/relational.test.mjs tests/model/snapshot.test.mjs",
    "test:data": "node --test scripts/fetch-weather.test.mjs scripts/evt-names.test.mjs",
    "snapshots:update": "node tests/model/update-snapshots.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
